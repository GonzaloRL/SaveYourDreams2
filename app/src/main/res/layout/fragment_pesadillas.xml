<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.gonzalo.saveyourdreams.Layout.FragmentoDream">

    <EditText
        android:layout_toLeftOf="@+id/miboton"
        android:layout_alignBaseline="@id/miboton"
        android:layout_alignParentLeft="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:hint="Buscar"
        android:ems="10"
        android:id="@+id/editText" />

    <ImageButton
        android:id="@+id/miboton"
        android:layout_alignParentRight="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_search_black_24dp"/>

    <View
        android:id="@+id/separador"
        android:layout_below="@id/miboton"
        android:layout_marginTop="1dp"
        android:layout_marginBottom="1dp"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/black"
        ></View>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/lista_pesadilla"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/separador"
        android:layout_alignParentBottom="true">

        <requestFocus/>

    </android.support.v7.widget.RecyclerView>

</RelativeLayout>
